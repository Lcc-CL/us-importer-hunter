"""Seed data: demo, test and initialization datasets.

One module per entity (demo_company, demo_contact, ...). Modules hold
typed data constants aligned with specs/*.yaml; the seed runner that
writes them through repositories lands together with the first models
(Sprint 2) and will be invoked via a script in apps/backend/scripts/.
"""
