"""Domain layer: pure business entities, value objects and rules.

Framework-free by design — no SQLAlchemy, no FastAPI, no OpenAI imports.
Services map between domain objects and ORM models / API schemas.
"""
