"""Browser tool: browser automation infrastructure.

Playwright / CDP drivers and browser session management live here;
other tools (google, linkedin, importyeti, website) build on it when
they need a real browser instead of plain HTTP.
"""
