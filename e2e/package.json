{
  "name": "us-importer-hunter-e2e",
  "version": "0.1.1",
  "private": true,
  "description": "Browser E2E regression for US Importer Hunter. Isolated from the Next.js app's dependency tree on purpose.",
  "scripts": {
    "test": "playwright test",
    "test:real": "playwright test --grep @real",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@types/node": "^20"
  }
}
